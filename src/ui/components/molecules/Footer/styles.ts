import styled from 'styled-components'
import T from 'ui/theme'

export const Wrapper = styled.footer`
  display: flex;
  width: 100%;
  justify-content: center;
  border-top: 1px solid ${T.colors.grey};
  padding: ${T.spacings.s};
`
